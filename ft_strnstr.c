char * ft_strnstr(const char *str, const char *pattern, size_t len)
{
    size_t i;

    i = 0;
    while (str !='/0' )
    if (str = pattern && i< len)
        str++;
        pattern++;
        return(pattern);
    elsereturn (0);
}